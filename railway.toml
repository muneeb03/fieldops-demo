# Railway service for Socket.io only (deploy Next.js separately on Vercel)
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start:socket"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
