generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Minimal schema — geofence check uses raw PostGIS queries only
model Placeholder {
  id Int @id @default(autoincrement())
}
